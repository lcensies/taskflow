> Pass note (transcript-viewer slice): phase 1, 2.3, 2.4, 3.1, 3.2 done; 2.1, 2.2, 3.3 and 4.x deliberately left unimplemented, as are 3.1's fingerprint/persist bullets and 3.2's fan-out sub-rows + label rows (they need 2.1/4.1).

## 1. Core engine (taskflow-core) [parallel]

- [x] 1.1 Add optional `label` to `PhaseSchema` in `packages/taskflow-core/src/schema.ts` (string, 1–120 chars, no newline; validation error `Phase '<id>': label must be …`), export nothing new; add `packages/taskflow-core/test/phase-label.test.ts` asserting valid/invalid labels and that `hashFlowIR` and a phase's cache `inputHash` are identical with and without a label. Verify: `pnpm test` passes.
- [x] 1.2 Add `onRawLine?: (line: string) => void` to `runSubagentProcess` options in `packages/taskflow-core/src/runner-core.ts`, invoked for every complete stdout line before `foldLine` (after the oversized-line guard); a throwing sink is caught once and disabled. Add `packages/taskflow-core/src/transcript.ts` exporting `parseTranscript(text): TranscriptEntry[]` (entries: `attempt`, `text{role}`, `tool_call{name,summary,args}`, `tool_result{name,text,isError}`; tolerant of a partial trailing line; reuse `summarizeToolCall` by exporting it) and re-export from `index.ts`. Verify: new `runner-core` test asserts `onRawLine` receives each line and a throwing sink does not fail the run; `transcript.test.ts` parses a fixture NDJSON with text, tool call, tool result and a truncated last line.
- [x] 1.3 Add to `packages/taskflow-core/src/store.ts`: `transcriptDirFor(runsRoot, flowName, runId)` → `<flowRunDir>/<runId>/`, `transcriptFileFor(dir, nodeId)` → `<dir>/<nodeId>.ndjson`, `listTranscripts(cwd, runId)` → nodeIds; remove the directory during run cleanup next to the `.trace.jsonl` removal using `removeArtifactDirectoryInsideRunsRoot`. Verify: store test creates a run + transcript file, runs cleanup, asserts directory gone.
- [x] 1.4 Add `transcriptDir?: string` to `RuntimeDeps` and `transcriptFile?: string` to `RunOptions` (`host/runner-types.ts`); in `runtime.ts` resolve `transcriptFile = transcriptFileFor(transcriptDir, nodeId)` at the same place `steerFile` is resolved so phases, map/parallel items, tournament variants and judges each get their own node file; before each retry attempt (>1) append `{"type":"taskflow_attempt","attempt":N,"at":<ms>}` to the file (fail-open). Verify: runtime test with a stub runner asserts the `transcriptFile` passed for a map phase's items is `<dir>/<phase>-0.ndjson`, `<dir>/<phase>-1.ndjson`, and that a retried phase's file contains one attempt marker. {needs: 1.3}
- [x] 1.5 Extend `packages/taskflow-core/src/peek.ts`: `PeekOptions.transcript?: boolean`; when set, read the node's transcript (phase id, or `<phase>-<item>` with `--item`), render entries as plain text (`parseTranscript` + a plain `formatTranscript(entries)` in `transcript.ts`), tail-truncate to the limit; `listPhases` shows `label ?? id` with the id dimmed-in-text (`id · label`). Verify: peek test with a fixture run + transcript file asserts `--transcript` output and truncation-from-front.

## 2. Pi host — independent pieces [parallel]

- [ ] 2.1 Flicker: in `packages/pi-taskflow/src/render.ts` (a) move spinner/elapsed/cost from the header to a new last-line footer, header shows static `▸` while running; (b) running rows use a static `◐` glyph; (c) `renderProgress(state, theme, opts?: {width?: number; maxRows?: number})` truncates every line with `truncateToWidth(width)` when width is given, and in collapsed mode caps rows (default 14) by keeping running/failed rows ± 1 neighbour and folding the rest into `… N done · M pending` lines; (d) rows show `phase.label ?? phase.id` (label column ≤ 40 cells, `…`), `↳ deps` keep ids; (e) `renderRunResult` returns a component whose `render(width)` calls `renderProgress` with that width instead of pre-built `Text` so truncation uses the real width; (f) export `renderFingerprint(state): string` — phase statuses + liveText + usage turns + footer second. Verify: `render.test.ts` asserts no line exceeds a 40-col width for a flow with a 12-dep phase and 90-char liveText; a 30-phase collapsed render has ≤ 14 rows; two renders 100 ms apart of an unchanged state differ only in the last line; a labelled phase shows its label.
- [ ] 2.2 `/tf runs` refresh in `packages/pi-taskflow/src/runs-view.ts`: replace `hasChanged` with a per-run fingerprint (`status|updatedAt|` + per-phase `status:liveText:usage.turns`); `poll()` always invalidates + `requestRender()` when any listed run is `running`; merge `getActiveRun()?.state` (from `./active-run.ts`) over the disk copy with the same `runId` inside `poll()`/constructor so the same-process run is shown live. Verify: `runs-view.test.ts` asserts a running run with unchanged disk state still triggers `requestRender` on poll, and that an active-run state overrides the disk copy.
- [x] 2.3 Transcript tee in `packages/pi-taskflow/src/runner.ts`: when `opts.transcriptFile` is set, `mkdirSync` its dir, open an append `WriteStream`, pass `onRawLine` writing `line + "\n"`; cap at 16 MiB (`PI_TASKFLOW_TRANSCRIPT_MAX_BYTES` overrides) then write one `{"type":"taskflow_truncated"}` line and stop; stream errors disable writing; close the stream when the process ends. Verify: runner test with the existing fake-pi child fixture asserts the file contains the child's stdout lines verbatim, and an unwritable path does not change the `RunResult`.
- [x] 2.4 New `packages/pi-taskflow/src/scroll-pane.ts` (`ScrollPane`: `offset`, `follow`, `handleKey(data): boolean` for ↑↓/j/k/PgUp/PgDn/Home/End/g/G, `view(lines, visible): string[]` with `↑ N more` / `↓ N more` indicator lines; follow sticks to bottom, manual up clears it, End/G restores) and `packages/pi-taskflow/src/transcript-view.ts` (`renderTranscript(entries, width, theme): string[]` — assistant text wrapped to width, tool calls as `▸ <summary>`, tool results dim and capped at 40 lines with `… (+N lines)`, every line `truncateToWidth`; `TranscriptTail` helper that re-reads a file from a byte offset when `statSync().size` grew and returns accumulated entries). Verify: `scroll-pane.test.ts` covers clamping, follow semantics and indicators; `transcript-view.test.ts` asserts width safety and result capping.

## 3. Pi host — wiring [sequential]

- [x] 3.1 Wire in `packages/pi-taskflow/src/index.ts`: `runFlow` passes `transcriptDir: transcriptDirFor(runsDir(ctx.cwd), state.flowName, state.runId)` to `executeTaskflow`, calls `persistThrottled` from `onProgress` too, and `emit()` skips `onUpdate` when `renderFingerprint(state)` is unchanged since the last emit; `/tf peek` accepts `--transcript`; `openRunHistory`/`openInspector` pass `() => tui.terminal.rows` to the components. Verify: `pnpm --filter pi-taskflow typecheck` (or repo `tsc --noEmit`) passes and an existing e2e `.mts` run leaves `runs/<flow>/<runId>/<phase>.ndjson` on disk.
- [x] 3.2 Inspector transcript pane in `packages/pi-taskflow/src/inspector-view.ts`: constructor takes `rows: () => number` and a transcript resolver (`nodeId → file path`); detail view = header (`label ?? id` with dim id, status, model, usage, attempts) + `ScrollPane` over `renderTranscript(TranscriptTail.entries)`, tailed on the existing 250 ms timer; fan-out phases expose their items as sub-rows (`<phase>-<n>`) selectable with ←/→ in detail; falls back to the current activity/output block when no transcript file exists; `s` steer unchanged; phase list shows labels. Verify: `inspector-view.test.ts` renders a detail view from a fixture transcript, asserts scroll keys move the window, follow mode shows the last entry after the file grows, and width safety at 50 cols.
- [ ] 3.3 Runs-panel transcript in `packages/pi-taskflow/src/runs-view.ts`: detail view gets a phase cursor (↑↓ over phase rows, highlighted), Enter opens that phase's transcript in the same pane layout as the inspector (reuse `ScrollPane` + `TranscriptTail`), Esc returns to the run detail, then to the list; running phases keep tailing on the 1 s refresh; hint lines updated. Verify: `runs-view.test.ts` asserts Enter on a phase renders the transcript and Esc returns; width safety at 50 cols.

## 4. Generator, docs, release [sequential]

- [ ] 4.1 `~/.pi/agent/skills/openspec-taskflow/scripts/openspec2taskflow.py`: emit `"label": f"{number} {text}"` truncated to 80 chars at a word boundary with `…` for every task phase; keep the verification-tail ids as labels-less. Verify: the script's self-test (`python3 openspec2taskflow.py --selftest`) asserts a phase carries the expected label and the flow still passes `taskflow verify`.
- [ ] 4.2 Docs: document `label` in `skills-src/taskflow/core.md` (phase field table) and `configuration.md`, `--transcript` for `peek` and the transcript files' location/cap in `core.md` + `packages/pi-taskflow/README.md`; run `pnpm run build:skills`; add a CHANGELOG entry under Unreleased covering all four fixes. Verify: `git diff --stat` shows generated skill files updated and `grep -n label skills-src/taskflow/core.md` finds the new row.
- [ ] 4.3 Full check: `pnpm run typecheck && pnpm test`; then a manual run of a 20-phase inline flow in pi with `PI_DEBUG_REDRAW=1` shows no `firstChanged < viewportTop` full renders in `~/.pi/agent/pi-debug.log`, `/tf runs` ticks during the run, and Enter on a phase scrolls its transcript. Verify: commands green and the manual checklist recorded in the change's `verification.md`.
